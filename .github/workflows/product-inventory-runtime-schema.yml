name: Product Inventory Runtime Schema
on:
  push:
    branches:
      - fix/product-inventory-2026-09-01
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/product-inventory-2026-09-01
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python .github/scripts/product_inventory_runtime_schema.py
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm check
      - run: pnpm build
      - name: Commit changes
        run: |
          rm -f .github/workflows/product-inventory-runtime-schema.yml .github/scripts/product_inventory_runtime_schema.py
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A
          git commit -m "fix: ensure product inventory columns in production [skip ci]"
          git push origin HEAD:fix/product-inventory-2026-09-01
