export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// O Note Note utiliza somente autenticação local.
// Usuários são criados e administrados pelo Super Administrador.
export const getLoginUrl = () => "/login";
};
